abstract final class AudioConstants {
  /// Estimasi durasi per karakter Arab (ms)
  static const double msPerArabChar = 220;
  static const minAyatDuration = Duration(seconds: 2);
  static const maxAyatDuration = Duration(seconds: 30);
}
